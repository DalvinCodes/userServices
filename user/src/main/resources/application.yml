server:
  port: 8080
spring:
  application:
    name: user
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/user
    username: dalvincodes
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        globally_quoted_identifiers: true
    show-sql: true

